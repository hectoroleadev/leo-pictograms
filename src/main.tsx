import React from 'react';
import ReactDOM from 'react-dom/client';
import LeoPictogramApp from './LeoPictogramApp.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LeoPictogramApp />
  </React.StrictMode>
);
